// import { prisma } from "@/lib/prisma";

export class ReportingDBService {
  static async getReportData(studentId: string) {
    if(!studentId) return {};
    // Collect all data for report (grades, activities, etc.)
    return {};
  }
}
